name: Deploy to GitHub Pages

# �C���{���XPush��master�A����workflow
on:
  push:
    branches: [master, main]
    paths:
      - "Client/**"

jobs:
  deploy-to-github-pages:
    # �ϥ�ubuntu
    runs-on: ubuntu-latest
    steps:
      # �ϥ�checkout actions
      - uses: actions/checkout@v3

      # �w��.NET Core SDK
      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      # ����椸���ձM��
      - name: Run Unit Test
        run: dotnet test --no-build

      # �o�G�{����Release��Ƨ�
      - name: Publish .NET Core Project
        run: dotnet publish ${{ env.DOTNET_APP_PUBLISH_PROJECT_PATH }} -c Release -o release --nologo

      # �ק�index.html��base href -- �q"/"�אּ"/���}/"
      - name: Change base-tag in index.html from / to /page/
        run: sed -i 's/<base href="\/" \/>/<base href="\/BlazorApp\/" \/>/g' release/wwwroot/index.html

      # �ƻsindex.html���e��404.html
      - name: copy index.html to 404.html
        run: cp release/wwwroot/index.html release/wwwroot/404.html

      # �[�J�@��.nojekyll�ɮ�
      - name: Add .nojekyll file
        run: touch release/wwwroot/.nojekyll

      # �Nrelease/wwwroot���{���X�Apush��gh-pages����
      - name: Commit wwwroot to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4.3.3
        with:
          branch: gh-pages
          folder: release/wwwroot
